import { EmployeesfilterPipe } from './employeesfilter.pipe';

describe('EmployeesfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeesfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
